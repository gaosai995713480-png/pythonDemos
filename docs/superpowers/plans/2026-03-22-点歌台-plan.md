# 点歌台（Jukebox）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现点歌台功能，让访客无需登录即可搜索和推荐歌曲，附上昵称和祝福语，情侣一键采纳到歌单。

**Architecture:** 后端新增 `jukebox.py` 路由模块（4 个端点），复用现有 Meting 音乐搜索。数据库新增 2 张表。前端新增 `JukeboxView.vue` 页面，复用现有 `TopBar`、`GlassModal` 组件和 `musicApi.search`。

**Tech Stack:** FastAPI, pymysql, Vue 3 (Composition API), Pinia, Vite

---

## File Structure

| Action | Path | Responsibility |
|--------|------|---------------|
| Create | `backend/routers/jukebox.py` | 点歌台 API 路由（list/create/like/adopt） |
| Create | `frontend/src/views/JukeboxView.vue` | 点歌台页面 |
| Modify | `backend/config.py` | 新增 `song_request_table` 表名配置 |
| Modify | `backend/database.py` | `init_tables()` 新增 2 张表 DDL |
| Modify | `backend/main.py` | 注册 jukebox 路由 |
| Modify | `frontend/src/api/index.js` | 新增 `jukeboxApi` 导出 |
| Modify | `frontend/src/router/index.js` | 新增 `/jukebox` 路由 |
| Modify | `frontend/src/views/HomeView.vue` | 导航网格新增入口 |

---

## Task 1: 后端配置与数据表

**Files:**
- Modify: `backend/config.py:30-32`
- Modify: `backend/database.py:66-170`

- [ ] **Step 1: 在 config.py 新增表名配置**

在 `backend/config.py` 的表名区域新增：

```python
song_request_table: str = os.getenv("TABLE_SONG_REQUEST", "love_song_requests")
```

- [ ] **Step 2: 在 database.py 的 init_tables() 新增建表 SQL**

在 `backend/database.py` 的 `tables` 字典中新增两张表：

```python
settings.song_request_table: f"""
    CREATE TABLE IF NOT EXISTS `{settings.song_request_table}` (
        id BIGINT AUTO_INCREMENT PRIMARY KEY,
        song_id VARCHAR(20) NOT NULL,
        song_name VARCHAR(100) NOT NULL,
        artist VARCHAR(100) NOT NULL,
        platform VARCHAR(20) NOT NULL DEFAULT 'netease',
        nickname VARCHAR(30) DEFAULT '匿名访客',
        message VARCHAR(100) DEFAULT '',
        likes INT NOT NULL DEFAULT 0,
        ip VARCHAR(45) NOT NULL,
        is_adopted TINYINT NOT NULL DEFAULT 0,
        created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
    ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4
""",
f"{settings.song_request_table}_likes": f"""
    CREATE TABLE IF NOT EXISTS `{settings.song_request_table}_likes` (
        id BIGINT AUTO_INCREMENT PRIMARY KEY,
        request_id BIGINT NOT NULL,
        ip VARCHAR(45) NOT NULL,
        created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
        UNIQUE KEY uniq_request_ip (request_id, ip)
    ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4
""",
```

- [ ] **Step 3: 验证数据库初始化**

Run: `cd d:\pythonDemo\pythonDemo && python -c "from backend.database import init_tables; init_tables(); print('OK')"`
Expected: `OK`（无报错）

---

## Task 2: 后端点歌台路由

**Files:**
- Create: `backend/routers/jukebox.py`
- Modify: `backend/main.py:18,49`

- [ ] **Step 1: 创建 jukebox.py 路由文件**

```python
"""点歌台路由 — 访客推荐歌曲"""
import re
import time
from collections import defaultdict
from fastapi import APIRouter, Request, Depends
from ..database import get_db
from ..config import settings
from ..dependencies import require_auth
from ..services.meting import METING_PLATFORMS

router = APIRouter(prefix="/api", tags=["jukebox"])

# IP 限频：{ip: [timestamp, ...]}
_ip_timestamps: dict[str, list[float]] = defaultdict(list)
_RATE_LIMIT = 5        # 每小时最多推荐次数
_RATE_WINDOW = 3600     # 窗口（秒）


def _check_rate_limit(ip: str) -> bool:
    """检查 IP 是否超过限频，返回 True 表示允许"""
    now = time.time()
    timestamps = _ip_timestamps[ip]
    # 清理过期记录
    _ip_timestamps[ip] = [t for t in timestamps if now - t < _RATE_WINDOW]
    return len(_ip_timestamps[ip]) < _RATE_LIMIT


@router.get("/jukebox")
def list_requests(limit: int = 50):
    limit = max(1, min(limit, 100))
    table = settings.song_request_table
    with get_db() as conn:
        with conn.cursor() as cursor:
            cursor.execute(
                f"SELECT id, song_id, song_name, artist, platform, nickname, message, likes, is_adopted, created_at "
                f"FROM `{table}` ORDER BY likes DESC, created_at DESC LIMIT %s",
                (limit,),
            )
            rows = cursor.fetchall()
    return [
        {
            "id": r[0], "song_id": r[1], "song_name": r[2], "artist": r[3],
            "platform": r[4], "nickname": r[5], "message": r[6],
            "likes": r[7], "is_adopted": bool(r[8]),
            "created_at": r[9].isoformat() if r[9] else None,
        }
        for r in rows
    ]


@router.post("/jukebox")
def create_request(body: dict, request: Request):
    song_id = str(body.get("song_id", "")).strip()
    song_name = str(body.get("song_name", "")).strip()
    artist = str(body.get("artist", "")).strip()
    platform = str(body.get("platform", "netease")).strip()
    nickname = str(body.get("nickname", "")).strip() or "匿名访客"
    message = str(body.get("message", "")).strip()

    if not song_id or not song_name:
        return {"error": "song_id and song_name are required"}, 400
    if platform not in METING_PLATFORMS:
        platform = "netease"

    # 长度限制
    nickname = nickname[:30]
    message = message[:100]
    song_name = song_name[:100]
    artist = artist[:100]

    client_ip = (
        request.headers.get("X-Real-IP")
        or request.headers.get("X-Forwarded-For", "").split(",")[0].strip()
        or request.client.host
    )

    if not _check_rate_limit(client_ip):
        return {"error": "推荐太频繁，请稍后再试", "rate_limited": True}

    table = settings.song_request_table
    with get_db() as conn:
        with conn.cursor() as cursor:
            cursor.execute(
                f"INSERT INTO `{table}` (song_id, song_name, artist, platform, nickname, message, ip) "
                f"VALUES (%s, %s, %s, %s, %s, %s, %s)",
                (song_id, song_name, artist, platform, nickname, message, client_ip),
            )
            row_id = cursor.lastrowid
        conn.commit()

    _ip_timestamps[client_ip].append(time.time())
    return {"ok": True, "id": row_id}


@router.post("/jukebox/like")
def like_request(body: dict, request: Request):
    try:
        request_id = int(body.get("id", 0))
    except (ValueError, TypeError):
        request_id = 0
    if request_id <= 0:
        return {"error": "invalid id"}, 400

    client_ip = (
        request.headers.get("X-Real-IP")
        or request.headers.get("X-Forwarded-For", "").split(",")[0].strip()
        or request.client.host
    )

    table = settings.song_request_table
    like_table = f"{table}_likes"

    with get_db() as conn:
        with conn.cursor() as cursor:
            cursor.execute(
                f"INSERT IGNORE INTO `{like_table}` (request_id, ip) VALUES (%s, %s)",
                (request_id, client_ip),
            )
            if cursor.rowcount == 0:
                cursor.execute(
                    f"SELECT likes FROM `{table}` WHERE id = %s", (request_id,)
                )
                row = cursor.fetchone()
                return {"ok": True, "id": request_id, "likes": row[0] if row else 0, "liked": False}

            cursor.execute(
                f"UPDATE `{table}` SET likes = likes + 1 WHERE id = %s", (request_id,)
            )
            conn.commit()
            cursor.execute(
                f"SELECT likes FROM `{table}` WHERE id = %s", (request_id,)
            )
            row = cursor.fetchone()
    return {"ok": True, "id": request_id, "likes": row[0] if row else 0, "liked": True}


@router.post("/jukebox/adopt")
def adopt_request(body: dict, _=Depends(require_auth)):
    try:
        request_id = int(body.get("id", 0))
    except (ValueError, TypeError):
        request_id = 0
    if request_id <= 0:
        return {"error": "invalid id"}, 400

    table = settings.song_request_table
    with get_db() as conn:
        with conn.cursor() as cursor:
            cursor.execute(
                f"SELECT song_id, song_name, artist, platform, is_adopted FROM `{table}` WHERE id = %s",
                (request_id,),
            )
            row = cursor.fetchone()
            if not row:
                return {"error": "not found"}, 404
            if row[4]:
                return {"error": "already adopted"}

            # 插入到歌单
            cursor.execute(
                f"INSERT INTO `{settings.music_table}` (song_name, artist, netease_id, platform) "
                f"VALUES (%s, %s, %s, %s)",
                (row[1], row[2], row[0], row[3]),
            )
            # 标记已采纳
            cursor.execute(
                f"UPDATE `{table}` SET is_adopted = 1 WHERE id = %s", (request_id,)
            )
        conn.commit()
    return {"ok": True}
```

- [ ] **Step 2: 在 main.py 注册路由**

在 `backend/main.py` 的 import 行新增：
```python
from .routers import auth, danmu, timeline, capsule, mood, wish, map, music, weather, photos, config, jukebox
```

在路由注册区新增：
```python
app.include_router(jukebox.router)
```

- [ ] **Step 3: 验证后端启动无报错**

Run: `cd d:\pythonDemo\pythonDemo && python -c "from backend.main import app; print('App created OK')"`
Expected: `App created OK`

---

## Task 3: 前端 API 层

**Files:**
- Modify: `frontend/src/api/index.js`

- [ ] **Step 1: 新增 jukeboxApi 导出**

在 `frontend/src/api/index.js` 的 `configApi` 之后新增：

```javascript
export const jukeboxApi = {
  list: (limit = 50) => get(`/api/jukebox?limit=${limit}`),
  create: (data) => post('/api/jukebox', data),
  like: (id) => post('/api/jukebox/like', { id }),
  adopt: (id) => post('/api/jukebox/adopt', { id }),
}
```

---

## Task 4: 前端路由与首页入口

**Files:**
- Modify: `frontend/src/router/index.js:46-50`
- Modify: `frontend/src/views/HomeView.vue:89-92`

- [ ] **Step 1: 新增 /jukebox 路由**

在 `frontend/src/router/index.js` 的 `Map` 路由之后、`pathMatch` 之前新增：

```javascript
{
  path: '/jukebox',
  name: 'Jukebox',
  component: () => import('../views/JukeboxView.vue'),
},
```

- [ ] **Step 2: 首页导航网格新增入口**

在 `frontend/src/views/HomeView.vue` 的导航网格中，`恋爱地图` 之后新增：

```html
<router-link to="/jukebox" class="nav-card">
  <span class="nav-icon">🎤</span><span class="nav-label">点歌台</span>
</router-link>
```

---

## Task 5: 前端点歌台页面

**Files:**
- Create: `frontend/src/views/JukeboxView.vue`

- [ ] **Step 1: 创建完整的 JukeboxView.vue**

创建 `frontend/src/views/JukeboxView.vue`，包含：
- 搜索歌曲区域（平台选择 + 搜索框 + 搜索结果）
- 推荐弹窗（昵称 + 祝福语 + 确认按钮）
- 推荐榜列表（歌曲信息 + 推荐人 + 点赞 + 采纳按钮）
- 复用 TopBar、GlassModal 组件
- 复用现有 CSS 变量和 glassmorphism 风格
- 响应式布局

核心逻辑：
1. `onMounted` 加载推荐列表
2. 搜索复用 `musicApi.search`
3. 点击搜索结果 → 弹出推荐表单
4. 提交推荐 → 调用 `jukeboxApi.create`
5. 点赞 → 调用 `jukeboxApi.like`
6. 采纳（需登录）→ 调用 `jukeboxApi.adopt`
7. 未登录时隐藏采纳按钮（通过 `useAuthStore` 判断）

---

## Task 6: 前端构建验证

- [ ] **Step 1: 运行 Vite 构建检查**

Run: `cd d:\pythonDemo\pythonDemo\frontend && npx vite build`
Expected: 构建成功，无错误

---

## Task 7: 集成验证

- [ ] **Step 1: 启动后端服务**

Run: `cd d:\pythonDemo\pythonDemo && python -m uvicorn backend.main:app --port 8000`

- [ ] **Step 2: 浏览器手动验证**

1. 访问首页 → 确认导航网格有"🎤 点歌台"入口
2. 点击进入点歌台 → 页面正常渲染
3. 搜索歌曲 → 选择一首 → 填入昵称和祝福语 → 提交
4. 确认推荐出现在列表中
5. 对推荐点赞 → 确认点赞数增加
6. 登录后 → 确认出现"采纳"按钮
7. 点击采纳 → 确认歌曲出现在音乐页歌单中
